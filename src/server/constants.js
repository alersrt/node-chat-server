module.exports.event = {
  participant: {
    JOIN: 'join',
    LEFT: 'left',
  },
};

module.exports.actions = {
  channel: {
    CREATE: 'create',
    DESTROY: 'destroy',
    JOIN: 'join',
    LEAVE: 'leave',
  },
};